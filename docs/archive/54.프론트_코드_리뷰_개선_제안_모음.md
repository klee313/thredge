# 개선 제안 모음 (계획문서 54)

- 기준: `docs/archive/54.프론트_코드_리뷰_재수행_계획_9.md`
- 항목 수: 11
- 검토 일자: 2026-01-22

## frontend/src/App.tsx
- 발견 이슈: 로그아웃 실패 시 사용자 피드백 경로가 없음(에러 핸들러 부재)
- 개선 제안: 로그아웃 mutation에 onError 추가 및 글로벌 에러 연동
- 우선순위: 중
- 수정 여부: 완료
- 근거: logoutMutation에 onError 추가 및 글로벌 에러 연동

## frontend/src/routes/AdminRoute.tsx
- 발견 이슈: 인증 401도 `isError` 경로로 처리돼 "로그인 필요" 대신 에러로 노출될 가능성
- 개선 제안: 401 상태를 구분하거나 `authQuery` error를 로그인 처리 경로로 분기
- 우선순위: 중
- 수정 여부: 완료
- 근거: authQuery.error가 401일 때 로그인 필요 메시지로 분기

## frontend/src/components/home/ThreadCardContainer.tsx
- 발견 이슈: Props 매핑이 과도하게 많아 유지보수 난이도 상승 가능
- 개선 제안: 컨테이너 단위 memoization 또는 컨텍스트 분리 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: memo 적용 + data/uiProps/cardActions useMemo로 분리됨

## frontend/src/components/home/ThreadCardView.tsx
- 발견 이슈: UI/액션 props 범위가 매우 넓어 변경 영향 파악이 어려움
- 개선 제안: 하위 컴포넌트 책임 분리/메모화 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: 컴포넌트 memo 적용됨

## frontend/src/hooks/useHomeFeedController.ts
- 발견 이슈: 함수가 과도하게 비대해 책임 분리가 어려움
- 개선 제안: hooks 분할 또는 selectors/memoization 도입 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: state/actions/mutations/ui 반환 객체에 useMemo/ useCallback 적용

## frontend/src/hooks/useEntryActions.ts
- 발견 이슈: `invalidateEntryKeys`가 feed/search를 항상 invalidate → 이동/편집 잦을 때 과다 요청 가능
- 개선 제안: 변경 유형에 따라 invalidate 범위를 줄이는 옵션 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: invalidateTargets 옵션과 includeFeed/includeSearch 분기 추가됨

## frontend/src/hooks/useThreadDetailController.ts
- 발견 이슈: useEffect에서 setEntryComposerFocusId를 상태 기반으로 직접 호출(의도된 UX지만 렌더 의존성 높음)
- 개선 제안: 포커스 트리거를 view에서 분리하거나 조건 강화 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: autofocus 조건 강화 + 반환 객체 memoization 적용됨

## frontend/src/lib/api.ts
- 발견 이슈: 비 JSON 응답에 대해 내용 기반 파싱(문자열/JSON 혼합)이 있어 API 계약이 불명확하면 오류 가능
- 개선 제안: 엔드포인트별 응답 타입을 더 엄격히 구분하거나 requestJson의 처리 규칙 명확화
- 우선순위: 중
- 수정 여부: 완료
- 근거: requestJson 기본 JSON만 허용하고 allowNonJson 옵션으로 규칙 명확화

## frontend/src/lib/threadCache.ts
- 발견 이슈: feed/search에 대해 broad invalidation/update가 많아 대규모 데이터 시 비용 가능
- 개선 제안: 대상 쿼리 범위를 좁히는 옵션 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: includeFeed/includeSearch 옵션으로 범위 제어 추가됨

## frontend/src/lib/highlightMatches.tsx
- 발견 이슈: 서버가 신뢰되지 않는 HTML을 반환할 경우 XSS 위험
- 개선 제안: 신뢰 모델 변경 시 서버-side sanitization 및 마이그레이션 절차 명시 필요
- 우선순위: 중
- 수정 여부: 완료
- 근거: VITE_TRUSTED_HTML 플래그 + HTML 렌더링 경고/주석 추가됨

## frontend/src/locales/ko.json
- 발견 이슈: `home.pin`/`home.unpin`이 “보관/보관 해제”로 번역되어 기능 의미와 불일치 가능
- 개선 제안: 핀 고정 의미에 맞는 번역으로 조정 검토
- 우선순위: 중
- 수정 여부: 완료
- 근거: `home.pin`/`home.unpin`이 핀 고정 의미로 수정됨
