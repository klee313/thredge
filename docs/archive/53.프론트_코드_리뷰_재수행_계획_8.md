# 프론트 코드 리뷰 재수행 계획 8

## 배경/문제
- 37번 문서 기준의 단계별 리뷰를 모두 수행했으나 추가 검토 필요성이 남아 있음
- 기존 후속 문서(38-51번)를 참고하지 않고 동일한 절차로 다시 점검해 누락 항목 여부를 확인해야 함

## 목표
- 37번 문서의 단계별 리뷰를 동일한 기준으로 처음부터 재수행한다
- 추가 리스크/개선 포인트를 새로 식별한다
- 기존 정리와 중복되는 항목은 명확히 구분해 기록한다

## 범위
- 포함: `frontend` 디렉토리 전반(구조, 상태 관리, 컴포넌트, API 연동, 성능, 테스트)
- 제외: 백엔드/인프라/데이터 파이프라인 상세 코드 리뷰
- 제외: 38-51번 문서 내용 직접 참조

## 현재 동작 정리
- 37번 문서의 Step 0~Step 6 흐름을 그대로 사용한다
- 동일 대상 파일을 기준으로, 관찰 결과를 새롭게 기록한다
- 기존 리뷰 결과와의 중복 여부를 단계별로 분리 표기한다

## 개선 방향
- 동일한 관점으로 재검토하되, 누락 가능성이 높은 경계/예외/성능/상태 결합 지점을 우선 확인
- 리뷰 결과는 “신규 발견/중복 확인/추가 검토 필요”로 분류
- 재검토 범위를 명확히 기록해 추적 가능하도록 한다

## 단계별 계획
- Step 0: 리뷰 범위 고정 및 기본 문서 재확인
  - 대상 파일/폴더: `frontend/README.md`, `frontend/package.json`, `frontend/vite.config.ts`, `frontend/tsconfig*.json`, `frontend/eslint.config.js`, `frontend/tailwind.config.cjs`, `frontend/postcss.config.cjs`
  - 확인 포인트: 빌드/테스트 스크립트, 린트 규칙, 타입스크립트 경계
  - 완료 기준: 재검토 범위/규칙/빌드 전제 정리 및 기존 기록과의 중복 구분
- Step 1: 엔트리포인트/라우팅/전역 스타일 흐름 재정리
  - 대상 파일/폴더: `frontend/src/main.tsx`, `frontend/src/App.tsx`, `frontend/src/router.tsx`, `frontend/src/routes/*.tsx`, `frontend/src/index.css`, `frontend/src/assets/**`
  - 확인 포인트: 라우트 구조, 레이아웃 책임 분리, 전역 스타일 영향 범위
  - 완료 기준: 화면 진입 경로/라우트 맵 재정리 및 신규 발견 여부 표기
- Step 2: 핵심 화면/컴포넌트 구조 재점검
  - 대상 파일/폴더: `frontend/src/components/home/**`, `frontend/src/components/CategoryInlineCreator.tsx`, `frontend/src/components/common/**`
  - 확인 포인트: 컴포넌트 책임/props 경계, 재사용성, 과도한 상태 결합 여부
  - 완료 기준: 복잡도 높은 화면/컴포넌트 목록 업데이트 및 신규/중복 분리
- Step 3: 훅/상태 전파 경로 재점검
  - 대상 파일/폴더: `frontend/src/hooks/*.ts`, `frontend/src/hooks/*.tsx`, `frontend/src/store/settingsStore.ts`
  - 확인 포인트: 상태 소유권/결합, 사이드이펙트 위치, 훅 간 의존성
  - 완료 기준: 상태 관리 리팩토링 후보 재정리 및 누락 여부 확인
- Step 4: 데이터 계층 및 API 유틸 재점검
  - 대상 파일/폴더: `frontend/src/lib/api.ts`, `frontend/src/lib/api/generated.ts`, `frontend/src/lib/queryKeys.ts`, `frontend/src/lib/threadCache.ts`, `frontend/src/lib/useDebouncedValue.ts`, `frontend/src/lib/entryOrder.ts`, `frontend/src/lib/entryDepth.ts`
  - 확인 포인트: 캐시/쿼리 키 관리, 에러 처리 일관성, API 호출 책임 분리
  - 완료 기준: 데이터 계층 개선 포인트 재정리 및 신규 이슈 표기
- Step 5: UI 토큰/유틸/국제화 재점검
  - 대상 파일/폴더: `frontend/src/lib/uiTheme.ts`, `frontend/src/lib/uiTokens.ts`, `frontend/src/lib/mutedText.ts`, `frontend/src/lib/highlightMatches.tsx`, `frontend/src/i18n.ts`, `frontend/src/locales/**`, `frontend/src/lib/languages.ts`
  - 확인 포인트: 테마/타이포/컬러 일관성, 텍스트 처리, 번역 키 관리
  - 완료 기준: UI 유틸/국제화 리스크 재정리 및 신규 항목 구분
- Step 6: 결과 정리 및 우선순위 재제안
  - 변경 내용: 이슈 목록을 심각도/난이도/효익 기준으로 재분류
  - 완료 기준: 신규 발견 항목과 기존 중복 항목을 분리한 우선순위 제안

## 리스크 및 대응
- 기존 리뷰와 결과가 동일하여 추가 성과가 제한될 위험
  - 대응: 각 단계에서 누락 가능성이 높은 경계 조건을 별도 체크리스트로 운영
- 재검토 범위 확대에 따른 일정 지연 위험
  - 대응: Step 0에서 고정 범위를 명확히 확정하고 변경 시 즉시 기록

## 검증 계획
- 단계별로 “신규/중복/추가 검토 필요” 분류표를 작성한다
- 핵심 화면에 대해 최소한의 수동 동작 확인 절차를 제안한다

## 신규 발견 항목 및 작업 계획
- [x] `frontend/src/components/home/ThreadCategorySelector.tsx`: Enter 처리 시 기본 폼 submit 방지 및 입력 접근성(aria-label) 보완
- [x] `frontend/src/routes/ThreadDetailPage.tsx`: thread id 없음 처리(에러 표시) 추가
- [x] `frontend/src/App.tsx`/라우팅: 인증 에러 처리 책임 정리(401은 조용히 처리, 기타 오류는 글로벌 에러로 보고)
- [x] `frontend/src/components/home/ThreadCardContainer.tsx`: 엔트리/리플라이 제출 상태 분리(별도 뮤테이션 도입)
- [x] `frontend/src/components/CategoryInlineCreator.tsx`: 미사용 컴포넌트 삭제
- [x] `frontend/src/hooks/useHomeFeedUrlState.ts`: 유효하지 않은 카테고리 URL 정리 확인(컨트롤러에서 정규화 후 URL 갱신됨)
- [x] `frontend/src/hooks/useHomeFeedQueries.ts`: 검색 + 날짜 필터 중복 처리 확인(서버 미적용 대비 클라이언트 가드 주석 추가)
- [x] `frontend/src/hooks/useThreadDetailController.ts`: threadId 없음 흐름은 라우트에서 차단하도록 정리(컨트롤러는 string 전제)
- [x] `frontend/src/lib/api/generated.ts`: 생성 타입은 드리프트 체크 용도임을 문서화(README에 명시)
- [x] `frontend/src/lib/api.ts`: `requestJson` 빈 응답 반환 처리 점검(반환 void는 requestEmpty 사용 확인)
- [x] `frontend/src/lib/threadCache.ts`: feed/search 전체 업데이트 범위 확인(부분 키 포함 업데이트 의도)
- [x] `frontend/src/lib/queryKeys.ts`: `thread.detail` 키는 string 필수로 변경해 undefined 사용 방지
- [x] `frontend/src/lib/uiTheme.ts`: 프리셋 중복 제거(`original` 삭제)
- [x] `frontend/src/lib/uiTokens.ts`: 포커스 스타일 일관화(입력도 focusRing 사용)
